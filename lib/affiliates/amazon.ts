import { Provider, ProviderResult, SearchParams } from './unified'; export class AmazonProvider implements Provider{ name='amazon' as const; enabled(){return !!(process.env.AMAZON_PAAPI_ACCESS_KEY&&process.env.AMAZON_PAAPI_SECRET_KEY&&process.env.AMAZON_PAAPI_PARTNER_TAG);} async search(p:SearchParams):Promise<ProviderResult>{ if(!this.enabled()) return {provider:this.name,items:[]}; return {provider:this.name,items:[]}; } }
